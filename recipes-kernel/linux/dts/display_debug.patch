diff --git a/drivers/gpu/drm/panel/panel-ilitek-ili9881c.c b/drivers/gpu/drm/panel/panel-ilitek-ili9881c.c
index 10a8bf6d..10fc771e 100644
--- a/drivers/gpu/drm/panel/panel-ilitek-ili9881c.c
+++ b/drivers/gpu/drm/panel/panel-ilitek-ili9881c.c
@@ -731,6 +731,7 @@ static int ili9881c_dsi_probe(struct mipi_dsi_device *dsi)
 	const struct panel_desc *desc;
 	int ret;
 
+	printk("DEBUG: in ili9881c_dsi_probe\n");
 	ctx = devm_kzalloc(&dsi->dev, sizeof(*ctx), GFP_KERNEL);
 	if (!ctx)
 		return -ENOMEM;
@@ -748,6 +749,7 @@ static int ili9881c_dsi_probe(struct mipi_dsi_device *dsi)
     ctx->power_gpio = devm_gpiod_get(&dsi->dev, "power", GPIOD_OUT_LOW);
     if (IS_ERR(ctx->power_gpio)) {
 		dev_err(&dsi->dev, "failed to get mipi power gpio");
+		printk("DEBUG: ->return powerGPIO %d\n",PTR_ERR(ctx->power_gpio));
 		return PTR_ERR(ctx->power_gpio);
     } else {
 		gpiod_set_value_cansleep(ctx->power_gpio, 0);
@@ -780,6 +782,7 @@ static int ili9881c_dsi_probe(struct mipi_dsi_device *dsi)
 	ret = drm_panel_of_backlight(&ctx->panel);
 	if (ret) {
 		dev_err(&dsi->dev, "Failed to get backlight handle\n");
+		printk("DEBUG: return backlight handle %d\n", ret);
 		return ret;
 	}
 
@@ -789,6 +792,7 @@ static int ili9881c_dsi_probe(struct mipi_dsi_device *dsi)
 	if (ret < 0)
 		drm_panel_remove(&ctx->panel);
 
+	printk("DEBUG: end of probe, code %d",ret);
 	return ret;
 }
 
diff --git a/arch/arm64/boot/dts/renesas/rzg2l-smarc.dtsi b/arch/arm64/boot/dts/renesas/rzg2l-smarc.dtsi
index 146b84b856e6..7fddaefc3407 100644
--- a/arch/arm64/boot/dts/renesas/rzg2l-smarc.dtsi
+++ b/arch/arm64/boot/dts/renesas/rzg2l-smarc.dtsi
@@ -45,4 +45,22 @@ &ov5645 {
 	enable-gpios = <&pinctrl RZG2L_GPIO(2, 0) GPIO_ACTIVE_HIGH>;
 	reset-gpios = <&pinctrl RZG2L_GPIO(40, 2) GPIO_ACTIVE_LOW>;
 };
-
+&gpt5 {
+	pinctrl-0 = <&gpt5_pins>;
+	pinctrl-names = "default";
+	channel="channel_A";
+	poeg = <&poegd>;
+	status = "okay";
+};
+/*backlight: backlight {
+		compatible = "pwm-backlight";
+		pwms = <&gpt5 0 40000 0>;
+		brightness-levels = <0 8 32 64 96 128 160 192 224 255>;
+		default-brightness-level = <8>;
+		status = "disabled";
+	};*/
+&pinctrl {
+	gpt5_pins: gpt5 {
+		pinmux = <RZG2L_PORT_PINMUX(43, 2, 2)>; /* Channel A */
+	};
+};
diff --git a/arch/arm64/boot/dts/renesas/rz-smarc-common.dtsi b/arch/arm64/boot/dts/renesas/rz-smarc-common.dtsi
index ad00f7db7d14..4bf75cf678a7 100644
--- a/arch/arm64/boot/dts/renesas/rz-smarc-common.dtsi
+++ b/arch/arm64/boot/dts/renesas/rz-smarc-common.dtsi
@@ -101,6 +101,13 @@ ov5645_vddd_1v5: 1p5v {
 		regulator-max-microvolt = <1500000>;
 		regulator-always-on;
 	};
+	backlight: backlight {
+		compatible = "pwm-backlight";
+		pwms = <&gpt5 0 40000 0>;
+		brightness-levels = <0 8 32 64 96 128 160 192 224 255>;
+		default-brightness-level = <8>;
+		status = "okay";
+	};
 };
 
 &audio_clk1{
